class Firebasedata {
  String ?name;
  String  ? address;
  var ages;
  var contacts;
  Firebasedata({
    required this.name,
    required this.address,
    required this.ages,
    required this.contacts,
  });
}
