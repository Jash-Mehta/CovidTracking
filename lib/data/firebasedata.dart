class Firebasedata {
  String ? name;
  String ? address;
  String age;
  String contact;
  Firebasedata({
    required this.name,
    required this.address,
    required this.age,
    required this.contact,
  });
}
class HiveSendingdata {
  String ? name;
  String ? address;
  HiveSendingdata({
    required this.name,
    required this.address,
  });
}
